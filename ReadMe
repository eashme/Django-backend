使用Django的,后台管理系统,提供了数据库备份脚本,验证码破解预处理后调用打码平台,以及简单验证码的破解接口。

前端使用 lay-ui进行开发,后台使用Django框架,后端使用uwsgi和Nginx进行服务器搭建。

我对于前端只能说了解,所以前端界面写的不是很好，整个系统前后端从构思到代码实现加起来花费时间不超过3周,有些地方看起来比较low,但是暂时也没有改的必要,和精力。

程序入口文件还是manage.py文件, 命令行  输入 python manage.py runserver -h IP地址 -p 端口号即可开始运行

程序开始后访问路径  /static/template/login.html即可进入登录界面。
